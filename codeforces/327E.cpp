#include <cstdio>
#include <iostream>
using namespace std;
const int MOD = 1000000007;
int a[30];
int k1, k2;
int main()
{
    ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
    int n, k;
    cin >> n >> k;
    for (int i = 1; i <= n; ++i)
        cin >> a[i];
    if (k == 1)
    {
        cin >> k1;
        }
    else
    {
        cin >> k1 >> k2;
    }
    return 0;
}