#include <cstdio>
#include <iostream>
using namespace std;
const int MAXN = 150;
const int MAXM = 2000;
const int MOD = 31011;
struct gauss
{
    int n, G[MAXN][MAXN];
    void Gauss()
    {
        int ans = 1;
        for (int i = 1; i < n; ++i)
        {
            int idx = i;
        }
    }
};
int main()
{
    ios::sync_with_stdio(0), cin.tie(), cout.tie(0);
    int n, m;
    cin >> n >> m;

    return 0;
}